% Given matrix A, the rms-values for the columns of A are computed from "from"% to "to". If no from/to pair is given, the rms is computed for all columns% OH 22.10.97function [rms_vec] = rms(A, from, to);[m, n] = size(A);if nargin == 1     % If only a matrix is given	from = 1;	to = n;end;cnt=1;for i = from:to	rms_vec(cnt) = sqrt(A(:,i)'*A(:,i)/m); 	cnt = cnt+1;end;