% Compute angle matrix for rows of A (angles in degrees)% Absolute values of acos(correlation) are computed% Only columns from "from" to "to" are used (default: all columns)% If flag==1, the mean is removed from every row% OH 28.10.97% Uses auto_correl_matfunction C = auto_angle_mat(A, flag, from, to);if (nargin==1) | (nargin==2),	from = 1;	to = length(A(1,:));end;if (nargin==1),	flag = 0;end;C = auto_correl_mat(A, flag, from, to);C = (180/pi)*abs(acos(C));