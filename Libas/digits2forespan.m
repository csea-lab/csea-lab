function [schwelle] = digits2forespan(invec)% reads 16 element vector and outputs forward digit span% weights first two items by 1, other items by 2% alternativ vorwaerts: [schwelle] = digits2forespan(invec)if length(invec) ~= 16, error('falsche anzahl von nullen und einsen, nadine :-)'), endinvec(1:4) = invec(1:4).*1invec(5:16) = invec(5:16).*2;schwelle = (sum(invec)/28) .* 9; 