% Computes leadfield matrix for given dipole locations (loc_mat(3,nr_dip))% and sensor positions/orientations  (sens_mat(6, nr_sens))% For every dipole location, three leadfield components (xyz) are computed% => lfd_mat(nr_sens, 3*nr_dip)% OH 03.12.97% uses sphereMEG()function lfd_mat = lfd_MEG(loc_mat, sens_mat);[ml nl] = size(loc_mat);[ms ns] = size(sens_mat);for i=1:nl	for j=1:ns		lead = sphere_meg(sens_mat(1:3,j), sens_mat(4:6,j), loc_mat(:,i));		lfd_mat(j, 3*(i-1)+1:3*i) = lead; 	end;  % for jend;  % for i