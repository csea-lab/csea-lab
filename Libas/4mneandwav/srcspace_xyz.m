% Create sourcespace (dipole locations) in a sphere% Locations are equally spaced by distance% Sphere radius is "radius" (surprise, surprise!)% Locations with z<cut_bottom are excluded (default: -radius)% OH 23.10.97function [diploc] = srcspace_xyz(radius, distance, cut_bottom);if nargin==0,   disp(' [diploc] = srcspace_xyz(radius, distance, cut_bottom); ');end;if nargin == 2	cut_bottom = -radius;end;cnt = 1;for z = radius:-distance:cut_bottom	for x = -radius:distance:radius		for y = -radius:distance:radius			r = sqrt(x*x + y*y + z*z);			if r <= radius				diploc(1,cnt) = x;				diploc(2,cnt) = y;				diploc(3,cnt) = z;				cnt = cnt + 1;			end;   % if 		end;   % for y	end;    % for xend;   % for z