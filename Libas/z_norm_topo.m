% z_norm_topo% computes z normalization of time and electrode matrix for each time point, % with mean and std across all timepoints and electrodes% timepoints can be windows electrodes can be clustersfunction [normMat] = z_norm_topo(InMat); [NElecs, NTpoints] = size(InMat); ariMean_mat=mean(reshape(InMat, 1,NElecs * NTpoints));std_mat= std(reshape(InMat, 1,NElecs * NTpoints));for TPoint = 1 :  NTpoints; 	InMat(:, TPoint) = (InMat(:, TPoint) - ariMean_mat) ./ std_mat;endnormMat = InMat; 	